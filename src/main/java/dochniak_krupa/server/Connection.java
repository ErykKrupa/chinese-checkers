package dochniak_krupa.server;

//storing necessary info about all clients connection
public class Connection {

    /* length og that array defines number of players that are able to connect the server
     * default value is false*/
    boolean[] clientsConnectedToServer = new boolean[6];
}
